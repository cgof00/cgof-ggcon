name = "gestor-emendas"
pages_build_output_dir = "dist"
compatibility_date = "2024-12-12"

# Variáveis públicas (disponíveis em dev e prod)
[vars]
SUPABASE_URL = "https://dvziqcgjuidtkihoeqdc.supabase.co"

# SUPABASE_SERVICE_ROLE_KEY NUNCA deve ficar neste arquivo (ele é versionado).
# Produção: Cloudflare Dashboard > Pages > cgof-ggcon > Settings > Variables and Secrets (tipo "Secret")
#   ou: npx wrangler pages secret put SUPABASE_SERVICE_ROLE_KEY --project-name cgof-ggcon
# Local:    copie .dev.vars.example para .dev.vars (ignorado pelo git) e preencha.
